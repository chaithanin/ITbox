// Chart primitives now live in the shared component library so every dashboard
// and report renders identical, theme-safe, server-rendered charts.
export * from "@/components/charts";
export type { Segment, HBarRow } from "@/components/charts";
