# next-auth@5 beta declares an optional peer on @simplewebauthn/browser@^9
# (for its built-in WebAuthn provider, which we do not use). ITBox implements
# WebAuthn directly with @simplewebauthn v13, so relax peer resolution.
legacy-peer-deps=true
